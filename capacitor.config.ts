import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "s'Cool Family",
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
